def int add (int $a, int $b) {
    return $a + $b;
}

def float test (int $a, int $b) { 
    int $ret = 0;
    if ($a > $b) 
        $ret = $ret + add($a, $b);
        $a = $a - 1;
    
    return [float] $ret;
}

def int run(){
    return 0;
}